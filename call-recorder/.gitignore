# Whisper model (large binary — download separately, see CLAUDE.md)
model/

# Temporary transcripts (generated at runtime)
transcripts/
transcript_*.txt

# Python cache
__pycache__/
*.pyc
*.pyo
.venv/

# OS
.DS_Store
Thumbs.db

# Runtime log do autocapture (ruidoso, local)
autocapture.log
autocapture.paused

# Sidecars de gravacoes automaticas ainda nao classificadas
recordings/*.pending.json
stefan-chain.*
fidelidade-daniel.*
stefan-redo.*
