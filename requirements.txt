streamlit>=1.35.0
openai>=1.0.0
pyyaml>=6.0
requests>=2.31.0
pandas>=2.0.0
altair>=5.0.0
pbixray>=0.15.1  # used by scripts/audit_semantic_model.py (audit-modelo-semantico skill)
# vaultindex/ (indice de busca do vault, ADR 2026-09-03): embeddings ONNX locais, CPU. Sem torch.
onnxruntime>=1.20
tokenizers>=0.20
numpy>=1.26
# call-recorder/coach_grammar.py: deteccao de typo no registro escrito.
# Puro Python, dicionario embutido, offline - sem servico externo.
pyspellchecker>=0.8

# vaultsources/ (2026-09-10): o cano de conhecimento externo.
# yt-dlp entra como MODULO, nao como .EXE: o executavel congelado carrega o
# certifi embutido e ignora SSL_CERT_FILE, entao nao passa pela CA da rede
# NETZSCH (que inspeciona TLS). Ver vaultsources/net.py.
yt-dlp>=2026.8
# youtube-transcript-api >= 1.2: a 1.0.3 devolvia ParseError no endpoint de
# legenda e o codigo antigo lia isso como "video sem legenda".
youtube-transcript-api>=1.2
# openpyxl: import do export de analytics de post do LinkedIn.
openpyxl>=3.1
